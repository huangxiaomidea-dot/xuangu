# 「别落了」微信小程序开发文档

> 一句话定位：出门前点开小程序，AI 结合你的生活规律、星期、出门时间和当天实际情况（天气、场景、时长），一秒告诉你**该带什么、别落下什么**。

- 文档版本：v1.0
- 状态：待评审 / 可直接进入开发
- 适用团队规模：1 前端 + 1 后端（可由 1 人全栈完成 MVP）

---

## 目录

1. [产品定位与核心场景](#1-产品定位与核心场景)
2. [用户流程](#2-用户流程)
3. [功能规格](#3-功能规格)
4. [信息架构与页面设计](#4-信息架构与页面设计)
5. [数据模型](#5-数据模型)
6. [后端 API 设计](#6-后端-api-设计)
7. [AI 推理层设计](#7-ai-推理层设计)
8. [习惯学习机制](#8-习惯学习机制)
9. [微信平台能力使用](#9-微信平台能力使用)
10. [隐私与合规](#10-隐私与合规)
11. [技术选型与工程结构](#11-技术选型与工程结构)
12. [性能、降级与容错](#12-性能降级与容错)
13. [成本估算](#13-成本估算)
14. [开发计划与里程碑](#14-开发计划与里程碑)
15. [测试与验收标准](#15-测试与验收标准)
16. [未来迭代方向](#16-未来迭代方向)

---

## 1. 产品定位与核心场景

### 1.1 要解决的问题

「出门忘带东西」是高频、低价值感但高痛感的问题：忘带钥匙要等锁匠，忘带伞要淋雨，忘带充电宝要焦虑一整天，周三忘带游泳卡就白跑一趟。

现有方案的问题：
- **待办 App**：需要手动录入，出门时根本想不起来打开。
- **固定清单**：不区分场景，周一上班和周六出游用同一张清单，很快就被忽略。
- **备忘录**：静态文本，不会因为「今天下雨」「今天要加班」而变化。

### 1.2 「别落了」的差异点

| 维度 | 传统清单 App | 别落了 |
|---|---|---|
| 触发方式 | 用户主动想起 | 出门前一键，2 秒出结果 |
| 清单来源 | 手动维护 | AI 生成 + 用户微调 |
| 是否考虑时间 | 否 | 星期几、出门时间、预计时长 |
| 是否考虑天气 | 否 | 是（温度、降水、风力、紫外线） |
| 是否学习习惯 | 否 | 是（每次勾选/忽略都会回流训练画像） |
| 输出形态 | 一长串待办 | 分级卡片：必带 / 建议带 / 可忽略 + 一句话理由 |

### 1.3 典型场景

| 场景 | 上下文 | 期望输出（示例） |
|---|---|---|
| 工作日早晨通勤 | 周二 08:15，晴转雷阵雨，地铁 | 必带：钥匙、手机、工牌；建议：折叠伞（下午 60% 降雨）、充电宝 |
| 周三晚游泳 | 周三 19:00，室内，2 小时 | 必带：泳镜、泳帽、门禁卡；建议：拖鞋、洗漱包（你上次落下过） |
| 周末带娃出游 | 周六 09:30，户外 6 小时，32℃ | 必带：水杯、防晒霜、湿巾、纸尿裤；建议：遮阳帽、备用衣物 |
| 临时出差 | 周四 06:40，异地 2 天 | 必带：身份证、充电器、笔电；建议：换洗衣物、常用药 |
| 看病 | 任意，医院 | 必带：医保卡、身份证、既往病历、现金 |

---

## 2. 用户流程

### 2.1 主流程（出门检查）

```
打开小程序
    │
    ├─ 首屏立即展示：一个巨大的「我要出门」按钮 + 当前时间/星期/天气
    │
    ▼
点击「我要出门」
    │
    ├─ [0ms]   本地规则引擎 + 上次同场景缓存 → 骨架清单立即渲染（不等网络）
    ├─ [300ms] 请求后端，携带上下文（时间/星期/位置粗粒度/最近一次场景）
    ├─ [~1.5s] AI 返回精修清单 → 平滑替换骨架，带高亮动画标出「新增/变化项」
    │
    ▼
清单页
    │
    ├─ 用户逐项勾选（勾选 = 已带）
    ├─ 长按某项 → 「今天不需要」/「以后别再提醒」
    ├─ 顶部可切换场景（上班/健身/出差/接娃/…）→ 触发重算
    ├─ 底部可调「预计出门时长」「是否会回家」→ 触发重算
    │
    ▼
点击「出发」
    │
    └─ 生成一次出门记录 → 反馈回流 → 更新用户画像
```

**关键设计原则：出门前的用户没有耐心。** 首屏到可用清单必须 ≤ 1 秒（走本地缓存），AI 精修结果是「渐进增强」，不是阻塞项。

### 2.2 首次使用（冷启动）流程

新用户没有任何历史数据，靠三步 30 秒建立初始画像：

1. **选身份标签**（多选）：上班族 / 学生 / 带娃家长 / 经常出差 / 健身 / 开车 / 骑车通勤
2. **选常带物品**（从预置 30 项里勾）：钥匙、手机、钱包、工牌、耳机、水杯、伞、充电宝……
3. **填固定规律**（可跳过）：例如「周三晚上游泳」「周一到周五 8:30 上班」

完成后立刻可用；后续通过使用反馈持续优化，不再打断用户。

---

## 3. 功能规格

### 3.1 MVP 范围（P0，必须有）

| ID | 功能 | 说明 |
|---|---|---|
| F-01 | 一键出门检查 | 首页大按钮，点击生成清单 |
| F-02 | 场景选择 | 预置场景 + 自定义场景，默认按时间/星期智能预选 |
| F-03 | 出门参数 | 出门时间（默认当前）、预计时长（<2h / 半天 / 全天 / 过夜）、是否回家、交通方式 |
| F-04 | AI 清单生成 | 分级输出：必带 / 建议带 / 可选，每项带一句理由 |
| F-05 | 物品库 | 用户可增删改自己的物品，支持分类和标签 |
| F-06 | 勾选与反馈 | 勾选已带、标记不需要，数据回流 |
| F-07 | 天气接入 | 城市级天气（温度、降水概率、风力、紫外线） |
| F-08 | 微信登录 | 静默登录建立用户，不强制授权头像昵称 |

### 3.2 P1（重要，MVP 后 2 周内）

| ID | 功能 | 说明 |
|---|---|---|
| F-09 | 规律管理 | 用户显式配置「周三晚游泳」这类周期规则 |
| F-10 | 习惯学习 | 从历史反馈自动挖掘规律，向用户确认后生效 |
| F-11 | 出门提醒 | 订阅消息，在常规出门时间前 15 分钟推送 |
| F-12 | 历史记录 | 查看过去的出门清单，一键「照上次再来一遍」 |
| F-13 | 落东西复盘 | 「我落了东西」按钮，记录后针对性提高该物品权重 |

### 3.3 P2（增强）

| ID | 功能 | 说明 |
|---|---|---|
| F-14 | 清单模板分享 | 生成海报 / 分享给家人（出差清单、带娃清单） |
| F-15 | 日历联动 | 用户手动录入当日行程标签（会议/面试/体检） |
| F-16 | 多人场景 | 「一家三口出门」，按成员维度合并清单 |
| F-17 | 语音输入 | 「今天要去医院」一句话生成清单 |

### 3.4 明确不做（划边界）

- ❌ 不做 IoT / 蓝牙防丢器联动（硬件成本与体验闭环不成立）
- ❌ 不做通用待办清单（会稀释「出门」这个核心场景）
- ❌ 不读取用户微信聊天/日历/通讯录（合规风险与信任成本过高）
- ❌ 不做持续后台定位（只在用户点击时获取一次粗粒度位置）

---

## 4. 信息架构与页面设计

### 4.1 页面结构

```
pages/
├── index/            首页 —— 出门按钮 + 天气 + 场景快捷入口
├── checklist/        清单页 —— 分级清单、勾选、参数调整
├── items/            物品库 —— 我的物品，分类管理
├── routine/          我的规律 —— 周期规则配置、AI 发现的规律确认
├── history/          出门记录 —— 历史清单、落东西记录
└── profile/          我的 —— 身份标签、提醒设置、隐私设置
```

### 4.2 首页布局（核心页，决定留存）

```
┌────────────────────────────────┐
│  周二 8:15        ☀️→🌧 22-29℃  │   ← 时间/星期/天气（一眼可见的上下文）
│  下午 60% 概率有雷阵雨            │
├────────────────────────────────┤
│                                │
│      ┌──────────────────┐      │
│      │                  │      │
│      │    我 要 出 门     │      │   ← 直径占屏宽 60% 的主按钮
│      │                  │      │      单手拇指可达区域
│      └──────────────────┘      │
│                                │
├────────────────────────────────┤
│  常用场景                        │
│  [ 🏢 上班 ] [ 🏊 游泳 ] [ 🛒 买菜 ]│   ← 跳过场景选择，直接出清单
├────────────────────────────────┤
│  💡 今天是周三，你通常晚上去游泳     │   ← 规律提示，点击直接切换场景
└────────────────────────────────┘
```

### 4.3 清单页布局

```
┌────────────────────────────────┐
│ ← 上班  ▾     8:15出门 · 全天 ▾  │  ← 场景与参数可即时改，改则重算
├────────────────────────────────┤
│ 🔴 必带 (3)                     │
│ ☐ 钥匙                          │
│ ☐ 手机                          │
│ ☐ 工牌      你上周三落下过        │
├────────────────────────────────┤
│ 🟡 建议带 (2)                    │
│ ☐ 折叠伞    下午 60% 概率雷阵雨    │
│ ☐ 充电宝    今天预计外出 9 小时    │
├────────────────────────────────┤
│ ⚪ 可选 (2)     ▾ 展开            │
├────────────────────────────────┤
│        [   出  发   ]           │  ← 提交反馈，收起小程序
└────────────────────────────────┘
```

**交互细节：**
- 每项右侧的灰色小字是「理由」，是 AI 输出的核心价值，不可省略。
- 长按弹出：`今天不需要` / `永久移除` / `提到必带`。
- 已勾选项自动置灰下沉，减少视觉噪音。
- AI 结果替换骨架清单时，新增项做一次 300ms 的高亮闪烁，让用户感知到「AI 想到了我没想到的」。

---

## 5. 数据模型

### 5.1 ER 概览

```
user 1──n item          用户的物品库
user 1──n routine       用户的周期规律
user 1──n outing        每次出门记录
outing 1──n outing_item 该次清单的每一项（含 AI 推荐结果与用户反馈）
user 1──n scene         场景（含预置与自定义）
```

### 5.2 表结构（PostgreSQL）

```sql
-- 用户
CREATE TABLE app_user (
    id              BIGSERIAL PRIMARY KEY,
    openid          VARCHAR(64) UNIQUE NOT NULL,
    unionid         VARCHAR(64),
    nickname        VARCHAR(64),                    -- 可为空，不强制授权
    tags            JSONB DEFAULT '[]',             -- ["上班族","带娃","开车"]
    city            VARCHAR(32),                    -- 城市级，不存精确坐标
    timezone        VARCHAR(32) DEFAULT 'Asia/Shanghai',
    created_at      TIMESTAMPTZ DEFAULT now(),
    updated_at      TIMESTAMPTZ DEFAULT now()
);

-- 场景（系统预置 user_id = NULL，用户自定义则填 user_id）
CREATE TABLE scene (
    id              BIGSERIAL PRIMARY KEY,
    user_id         BIGINT REFERENCES app_user(id) ON DELETE CASCADE,
    code            VARCHAR(32) NOT NULL,           -- work / gym / travel / kids / hospital
    name            VARCHAR(32) NOT NULL,
    icon            VARCHAR(16),
    is_preset       BOOLEAN DEFAULT false,
    sort_order      INT DEFAULT 0
);

-- 物品库
CREATE TABLE item (
    id              BIGSERIAL PRIMARY KEY,
    user_id         BIGINT NOT NULL REFERENCES app_user(id) ON DELETE CASCADE,
    name            VARCHAR(64) NOT NULL,
    category        VARCHAR(32),                    -- 证件/电子/衣物/健康/育儿/运动/其他
    -- 基础权重：用户手动置顶的必带项设为 1.0
    base_weight     NUMERIC(3,2) DEFAULT 0.50,
    -- 场景绑定：{"work": 0.9, "gym": 0.1}
    scene_weights   JSONB DEFAULT '{}',
    -- 条件触发：{"rain_prob_gte": 0.4} / {"temp_lte": 5} / {"duration_gte_hours": 6}
    conditions      JSONB DEFAULT '{}',
    is_archived     BOOLEAN DEFAULT false,
    created_at      TIMESTAMPTZ DEFAULT now(),
    UNIQUE (user_id, name)
);
CREATE INDEX idx_item_user ON item(user_id) WHERE is_archived = false;

-- 周期规律（用户显式配置 或 AI 挖掘后用户确认）
CREATE TABLE routine (
    id              BIGSERIAL PRIMARY KEY,
    user_id         BIGINT NOT NULL REFERENCES app_user(id) ON DELETE CASCADE,
    name            VARCHAR(64) NOT NULL,           -- "周三晚游泳"
    scene_code      VARCHAR(32),
    weekdays        SMALLINT[] NOT NULL,            -- {1,3,5}，1=周一
    start_minute    SMALLINT,                       -- 一天中的第 N 分钟，1140 = 19:00
    end_minute      SMALLINT,
    item_ids        BIGINT[] DEFAULT '{}',
    source          VARCHAR(16) DEFAULT 'user',     -- user / discovered
    confidence      NUMERIC(3,2),                   -- AI 挖掘时的置信度
    is_active       BOOLEAN DEFAULT true,
    created_at      TIMESTAMPTZ DEFAULT now()
);
CREATE INDEX idx_routine_user_active ON routine(user_id) WHERE is_active = true;

-- 出门记录
CREATE TABLE outing (
    id              BIGSERIAL PRIMARY KEY,
    user_id         BIGINT NOT NULL REFERENCES app_user(id) ON DELETE CASCADE,
    scene_code      VARCHAR(32),
    depart_at       TIMESTAMPTZ NOT NULL,
    weekday         SMALLINT NOT NULL,              -- 冗余，便于统计
    minute_of_day   SMALLINT NOT NULL,              -- 冗余
    duration_bucket VARCHAR(16),                    -- short/half_day/full_day/overnight
    transport       VARCHAR(16),                    -- walk/bike/metro/car/train/plane
    will_return     BOOLEAN DEFAULT true,
    weather         JSONB,                          -- 生成时的天气快照
    generated_by    VARCHAR(16),                    -- ai / rule / cache
    latency_ms      INT,
    created_at      TIMESTAMPTZ DEFAULT now()
);
CREATE INDEX idx_outing_user_time ON outing(user_id, depart_at DESC);

-- 清单项 + 反馈（核心训练数据）
CREATE TABLE outing_item (
    id              BIGSERIAL PRIMARY KEY,
    outing_id       BIGINT NOT NULL REFERENCES outing(id) ON DELETE CASCADE,
    item_id         BIGINT REFERENCES item(id) ON DELETE SET NULL,
    item_name       VARCHAR(64) NOT NULL,           -- 冗余快照，防物品被删
    level           VARCHAR(16) NOT NULL,           -- must / suggest / optional
    reason          VARCHAR(128),
    -- 用户反馈
    checked         BOOLEAN DEFAULT false,          -- 勾了 = 带了
    dismissed       BOOLEAN DEFAULT false,          -- 标记今天不需要
    forgotten       BOOLEAN DEFAULT false,          -- 事后回来标记「落下了」
    UNIQUE (outing_id, item_name)
);
CREATE INDEX idx_outing_item_outing ON outing_item(outing_id);

-- 天气缓存（按城市+小时，避免重复调外部 API）
CREATE TABLE weather_cache (
    city            VARCHAR(32) NOT NULL,
    hour_bucket     TIMESTAMPTZ NOT NULL,
    payload         JSONB NOT NULL,
    PRIMARY KEY (city, hour_bucket)
);
```

### 5.3 用户画像视图（供 AI 使用）

不额外建表，用物化视图按天刷新，作为 Prompt 的稳定输入：

```sql
CREATE MATERIALIZED VIEW user_item_stats AS
SELECT
    o.user_id,
    oi.item_name,
    o.scene_code,
    o.weekday,
    COUNT(*)                                            AS shown_count,
    SUM(CASE WHEN oi.checked   THEN 1 ELSE 0 END)       AS taken_count,
    SUM(CASE WHEN oi.dismissed THEN 1 ELSE 0 END)       AS dismissed_count,
    SUM(CASE WHEN oi.forgotten THEN 1 ELSE 0 END)       AS forgotten_count,
    ROUND(SUM(CASE WHEN oi.checked THEN 1 ELSE 0 END)::numeric
          / NULLIF(COUNT(*), 0), 2)                     AS take_rate
FROM outing o
JOIN outing_item oi ON oi.outing_id = o.id
WHERE o.created_at > now() - INTERVAL '180 days'
GROUP BY o.user_id, oi.item_name, o.scene_code, o.weekday;

CREATE UNIQUE INDEX ON user_item_stats (user_id, item_name, scene_code, weekday);
```

---

## 6. 后端 API 设计

统一前缀 `/api/v1`，鉴权用自签 JWT（由 `wx.login` 的 code 换取 openid 后签发），放在 `Authorization: Bearer <token>`。

### 6.1 接口清单

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/auth/login` | 用 `code` 换 token |
| POST | `/checklists:generate` | **核心接口**：生成出门清单 |
| POST | `/checklists/{id}/feedback` | 提交勾选/忽略反馈 |
| POST | `/checklists/{id}/forgotten` | 事后标记「落了什么」 |
| GET | `/items` / POST / PATCH / DELETE | 物品库 CRUD |
| GET | `/routines` / POST / PATCH / DELETE | 规律 CRUD |
| GET | `/routines/discovered` | AI 挖掘出的、待用户确认的规律 |
| GET | `/outings` | 历史记录（分页） |
| GET | `/context` | 首页上下文：天气 + 当前建议场景 + 规律提示 |

### 6.2 核心接口详情

**POST `/api/v1/checklists:generate`**

请求：

```json
{
  "scene_code": "work",
  "depart_at": "2026-08-18T08:15:00+08:00",
  "duration_bucket": "full_day",
  "transport": "metro",
  "will_return": true,
  "city": "杭州",
  "note": "下午有客户会议",
  "client_cache_key": "work-2-morning"
}
```

| 字段 | 必填 | 说明 |
|---|---|---|
| `scene_code` | 否 | 不传则由服务端按时间+星期+规律推断 |
| `depart_at` | 否 | 默认服务端当前时间 |
| `duration_bucket` | 否 | `short`(<2h) / `half_day` / `full_day` / `overnight` |
| `transport` | 否 | 影响随身重量与是否需要充电宝、雨具 |
| `will_return` | 否 | 影响「是否需要带备用衣物/钥匙」 |
| `city` | 否 | 城市名，用于天气；不传则用用户档案里的城市 |
| `note` | 否 | 自由文本，用户临时补充（AI 会重点参考） |

响应：

```json
{
  "outing_id": 90183,
  "scene": { "code": "work", "name": "上班" },
  "generated_by": "ai",
  "latency_ms": 1420,
  "context_summary": "周二 8:15 出门，杭州 22-29℃，下午 60% 概率雷阵雨，预计外出一整天",
  "groups": [
    {
      "level": "must",
      "items": [
        { "item_id": 11, "name": "钥匙",  "reason": "每次出门都需要" },
        { "item_id": 12, "name": "手机",  "reason": "每次出门都需要" },
        { "item_id": 18, "name": "工牌",  "reason": "你上周三落下过一次" }
      ]
    },
    {
      "level": "suggest",
      "items": [
        { "item_id": 25, "name": "折叠伞",  "reason": "下午 60% 概率雷阵雨" },
        { "item_id": 27, "name": "充电宝",  "reason": "预计外出 9 小时以上" }
      ]
    },
    {
      "level": "optional",
      "items": [
        { "item_id": 31, "name": "降噪耳机", "reason": "你地铁通勤时常带" }
      ]
    }
  ],
  "tips": "下午会议在 15:00，出门前确认笔电已充电"
}
```

**POST `/api/v1/checklists/{id}/feedback`**

```json
{
  "checked":   [11, 12, 18, 25],
  "dismissed": [31],
  "extra":     ["雨衣"]          // 用户临时手加的，自动入库
}
```

响应 `204 No Content`。这是学习闭环的唯一入口，必须保证幂等（同一 outing 可重复提交，以最后一次为准）。

---

## 7. AI 推理层设计

### 7.1 架构：规则引擎 + LLM 混合

纯 LLM 有三个问题：**慢**（1–3s）、**贵**、**不稳定**（同样输入可能给出不同清单）。纯规则的问题是：不会推理「今天 32℃ 带娃户外 6 小时 → 要带备用衣物」这类组合场景。

因此采用两层：

```
        ┌─────────────────────────────────────────┐
用户请求 →│  L1 规则引擎（本地/服务端，<50ms）          │→ 骨架清单（立即返回）
        │  · base_weight ≥ 0.9 的固定必带项         │
        │  · 命中 routine 的绑定物品                 │
        │  · 命中 conditions 的条件物品（雨/冷/长时）  │
        └─────────────────────────────────────────┘
                        │  规则结果 + 统计画像 + 天气 + 用户备注
                        ▼
        ┌─────────────────────────────────────────┐
        │  L2 LLM 精修（Claude，~1.5s）              │→ 最终清单（渐进替换）
        │  · 分级排序（must/suggest/optional）        │
        │  · 生成每项「一句话理由」                    │
        │  · 补充规则想不到的组合项                    │
        │  · 剔除明显冗余（如「不回家」时的家用物品）    │
        └─────────────────────────────────────────┘
```

L1 的结果**永远先返回**给客户端（HTTP 立即响应 or SSE 第一帧），L2 完成后通过第二帧 / 轮询补充。这样即使 LLM 超时或欠费，产品仍然可用。

### 7.2 模型选择

默认使用 **Claude Opus 5**（模型 ID `claude-opus-5`），配合较低的 `effort` 控制延迟与成本：

| 参数 | 取值 | 理由 |
|---|---|---|
| `model` | `claude-opus-5` | 生成质量与理由文案的可读性最好；1M 上下文，未来接入更长历史无压力 |
| `output_config.effort` | `"low"` 或 `"medium"` | 本任务是「结构化重排 + 短文案生成」，不需要深度推理。Opus 5 在 low/medium 档位表现依然很强，是控延迟/控成本的首要旋钮 |
| `thinking` | 不传（默认自适应开启） | Opus 5 默认开启 adaptive thinking；如需极致低延迟可显式 `{"type": "disabled"}`，但**必须同时保证 effort ≤ high**，否则接口报 400 |
| `max_tokens` | `2000` | 输出是结构化 JSON，不长；注意 `max_tokens` 同时约束思考 + 正文，别设太小导致截断 |
| `output_config.format` | JSON Schema | 结构化输出，免去解析和重试逻辑 |

> **成本敏感时的替代方案**：可切到 `claude-haiku-4-5`（$1/$5 per MTok，延迟更低）。建议做 A/B：用 Opus 5 跑「高价值场景」（出差、带娃、天气突变），用 Haiku 跑「日常通勤」这类模式固定的请求。这是产品侧的取舍，不建议一上来就为省钱统一降级——理由文案的质量直接决定用户是否信任 AI。

### 7.3 Prompt 结构（为缓存而设计）

Claude 的 prompt 缓存是**前缀匹配**：前缀里任何一个字节变化都会让后面全部失效。因此严格按「稳定 → 半稳定 → 易变」的顺序组织：

```
[1] system（全局稳定，所有用户共享）        ← 打 cache_control，命中率最高
    · 角色定义、分级规则、输出规范、文案风格

[2] messages[0] user（每用户稳定，按天变化）  ← 打 cache_control，按用户缓存
    · 用户身份标签、物品库全量、规律列表、历史统计摘要

[3] messages[0] user（本次请求，每次都变）    ← 不打断点
    · 当前时间/星期/天气/场景/时长/交通/用户备注
```

Claude Opus 5 的最小可缓存前缀是 **512 token**（比 Opus 4.8 的 1024 更低），我们的 [1]+[2] 通常在 1500–3000 token，稳定命中。

⚠️ **绝对不要**把「当前时间」拼进 system prompt —— 那会让每一次请求的缓存全部失效。这是本项目最容易踩的性能坑。

### 7.4 参考实现（Python / FastAPI）

```python
# app/ai/checklist.py
import json
from typing import Any

import anthropic

client = anthropic.Anthropic()  # 从 ANTHROPIC_API_KEY 或 ant auth login 配置读取

MODEL = "claude-opus-5"

# ── [1] 全局稳定：所有用户共享，缓存命中率最高 ────────────────────────────
SYSTEM_PROMPT = """你是「别落了」小程序的出门清单助手。用户在出门前打开小程序，你要在他迈出家门前告诉他该带什么。

分级规则：
- must（必带）：忘了会直接导致行程失败或严重不便。钥匙、证件、当次活动的核心装备属于此类。
- suggest（建议带）：忘了会不舒服但不致命，通常由天气、时长、交通方式触发。
- optional（可选）：锦上添花，用户可能已有替代方案。

理由文案要求：
- 每项必须给一条理由，不超过 14 个字，说清「为什么今天要带」。
- 优先引用具体依据：天气数值、历史行为、当次活动。例如「下午 60% 概率雷阵雨」优于「可能下雨」。
- 不要写「以防万一」「建议携带」这类没有信息量的话。
- 不要重复物品名本身。

清单规模：
- must 不超过 6 项，suggest 不超过 5 项，optional 不超过 4 项。清单越长越没人看。
- 用户 take_rate 低于 0.2 的物品不要放进 must。
- 用户标记过 forgotten 的物品，同场景下优先提升到 must，并在理由中提示。

只输出 JSON，不要任何解释性文字。"""


def _profile_block(profile: dict[str, Any]) -> str:
    """[2] 每用户稳定的画像块，按天刷新，可缓存。"""
    return json.dumps(profile, ensure_ascii=False, sort_keys=True, indent=None)


def _context_block(ctx: dict[str, Any]) -> str:
    """[3] 本次请求上下文，每次都变，放在最后。"""
    return json.dumps(ctx, ensure_ascii=False, sort_keys=True)


CHECKLIST_SCHEMA = {
    "type": "object",
    "properties": {
        "context_summary": {"type": "string"},
        "groups": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "level": {"type": "string", "enum": ["must", "suggest", "optional"]},
                    "items": {
                        "type": "array",
                        "items": {
                            "type": "object",
                            "properties": {
                                "name": {"type": "string"},
                                "reason": {"type": "string"},
                            },
                            "required": ["name", "reason"],
                            "additionalProperties": False,
                        },
                    },
                },
                "required": ["level", "items"],
                "additionalProperties": False,
            },
        },
        "tips": {"type": "string"},
    },
    "required": ["context_summary", "groups", "tips"],
    "additionalProperties": False,
}


def generate_checklist(profile: dict, ctx: dict, rule_draft: list[dict]) -> dict:
    """调用 Claude 精修规则引擎产出的草稿清单。"""
    response = client.messages.create(
        model=MODEL,
        max_tokens=2000,
        output_config={
            "effort": "low",                    # 延迟/成本的首要旋钮
            "format": {"type": "json_schema", "schema": CHECKLIST_SCHEMA},
        },
        system=[
            {
                "type": "text",
                "text": SYSTEM_PROMPT,
                "cache_control": {"type": "ephemeral"},   # 断点 1：全局共享
            }
        ],
        messages=[
            {
                "role": "user",
                "content": [
                    {
                        "type": "text",
                        "text": f"<user_profile>\n{_profile_block(profile)}\n</user_profile>",
                        "cache_control": {"type": "ephemeral", "ttl": "1h"},  # 断点 2：按用户
                    },
                    {
                        "type": "text",
                        # 易变内容放在最后一个断点之后，不影响前缀缓存
                        "text": (
                            f"<today>\n{_context_block(ctx)}\n</today>\n"
                            f"<rule_draft>\n{json.dumps(rule_draft, ensure_ascii=False)}\n</rule_draft>\n"
                            "请基于 rule_draft 精修，可增删，输出最终清单。"
                        ),
                    },
                ],
            }
        ],
    )

    # output_config.format 保证首个 text block 是合法 JSON
    text = next(b.text for b in response.content if b.type == "text")
    result = json.loads(text)

    # 观测：缓存命中率是本服务最重要的成本指标
    u = response.usage
    log_usage(
        cache_read=u.cache_read_input_tokens,
        cache_write=u.cache_creation_input_tokens,
        input_uncached=u.input_tokens,
        output=u.output_tokens,
    )
    return result
```

### 7.5 输入给 AI 的画像块（示例）

```json
{
  "tags": ["上班族", "地铁通勤", "健身"],
  "city": "杭州",
  "items": [
    {"name": "钥匙",   "category": "随身", "base_weight": 1.0},
    {"name": "工牌",   "category": "随身", "scene_weights": {"work": 0.95}},
    {"name": "泳镜",   "category": "运动", "scene_weights": {"gym": 0.9}},
    {"name": "折叠伞", "category": "随身", "conditions": {"rain_prob_gte": 0.4}}
  ],
  "routines": [
    {"name": "工作日通勤", "weekdays": [1,2,3,4,5], "start": "08:15", "scene": "work"},
    {"name": "周三游泳",   "weekdays": [3],         "start": "19:00", "scene": "gym"}
  ],
  "stats": [
    {"item": "工牌",   "scene": "work", "shown": 42, "take_rate": 0.93, "forgotten": 2},
    {"item": "充电宝", "scene": "work", "shown": 42, "take_rate": 0.31, "forgotten": 0},
    {"item": "降噪耳机","scene": "work","shown": 40, "take_rate": 0.78, "forgotten": 0}
  ]
}
```

### 7.6 上下文块（示例）

```json
{
  "now": "2026-08-18T08:15:00+08:00",
  "weekday": "周二",
  "is_holiday": false,
  "scene": "work",
  "duration_bucket": "full_day",
  "transport": "metro",
  "will_return": true,
  "note": "下午有客户会议",
  "weather": {
    "temp_min": 22, "temp_max": 29,
    "condition": "多云转雷阵雨",
    "rain_prob_afternoon": 0.6,
    "uv_index": 6,
    "wind_level": 3
  }
}
```

---

## 8. 习惯学习机制

### 8.1 三层学习，从轻到重

**第 1 层：统计权重（实时，无需模型）**

每次 feedback 落库后，异步更新 `user_item_stats`。核心指标：

```
take_rate(item, scene, weekday) = taken_count / shown_count
```

- `take_rate ≥ 0.85` 且 `shown_count ≥ 5` → 自动提升该物品在该场景的 `scene_weights` 至 0.9（进 must 候选）
- `take_rate ≤ 0.15` 且 `shown_count ≥ 8` → 降权至 0.1（沉到 optional 或不再展示）
- `forgotten_count ≥ 1` → 该物品在同场景下未来 30 天内强制进 must，理由带「你上次落下过」

**第 2 层：周期规律挖掘（每周离线跑一次）**

用简单频次统计找周期性，不需要 LLM：

```python
# 伪代码：找出「某物品在某星期几的携带率显著高于其他日子」
for item in user_items:
    baseline = take_rate(item, scene=None, weekday=None)
    for wd in range(1, 8):
        rate = take_rate(item, weekday=wd)
        n = shown_count(item, weekday=wd)
        # 至少 4 次观测，且高出基线 40 个百分点
        if n >= 4 and rate - baseline > 0.4:
            propose_routine(item, weekday=wd, confidence=rate)
```

挖掘出的规律**不直接生效**，进入 `routine` 表 `source='discovered'`，在「我的规律」页展示为待确认卡片：

> 💡 发现规律：你最近 5 个周三都带了泳镜和泳帽，要设为固定规律吗？ [ 是 ] [ 不用 ]

用户确认后 `is_active=true`。这一步很关键——**未经确认的自动规则会让用户感觉失控**。

**第 3 层：LLM 语义理解（按需）**

当用户在 `note` 里写自由文本（「今天去医院复查」「明天飞北京两天」），由 LLM 直接理解并映射到场景与物品，不入规则表，只作用于当次。

### 8.2 冷启动策略

新用户前 5 次出门，清单几乎完全来自：预置模板（按身份标签）+ 天气条件 + 用户初始勾选的常带物品。此阶段刻意保持清单**短**（must ≤ 4 项），避免第一印象是「一堆没用的东西」。

第 5 次之后统计量足够，开始个性化。

---

## 9. 微信平台能力使用

### 9.1 登录

```js
// utils/auth.js
export async function login() {
  const { code } = await wx.login()
  const { data } = await request({
    url: '/api/v1/auth/login',
    method: 'POST',
    data: { code },
    skipAuth: true,
  })
  wx.setStorageSync('token', data.token)
  return data
}
```

后端用 `code` 调 `https://api.weixin.qq.com/sns/jscode2session` 换 `openid`，签发自有 JWT（有效期 30 天，滑动续期）。

**不强制授权头像昵称**——出门清单不需要用户身份信息，减少授权弹窗对首次转化的损耗。

### 9.2 位置（可选授权）

只在用户点击「我要出门」且未设置城市时请求一次：

```js
// 类目需在小程序后台声明 scope.userLocation 用途
wx.getLocation({ type: 'wgs84' })
  .then(res => reverseGeocodeToCity(res.latitude, res.longitude))
  .catch(() => promptManualCitySelect())   // 拒绝授权则手动选城市，功能不阻塞
```

**只上报城市名，不上报经纬度。** 天气精度到城市级足够，且大幅降低合规负担。

### 9.3 订阅消息（出门提醒）

模板：`出行提醒` — 参数：出行时间、天气提示、提醒事项。

```js
// 在用户完成一次出门后，顺势请求订阅（此时意愿最高）
wx.requestSubscribeMessage({
  tmplIds: ['TEMPLATE_ID_GOING_OUT'],
  success(res) {
    if (res['TEMPLATE_ID_GOING_OUT'] === 'accept') {
      request({ url: '/api/v1/subscriptions', method: 'POST', data: { type: 'going_out' } })
    }
  },
})
```

服务端在用户常规出门时间前 15 分钟推送（依据 `routine.start_minute`），一次订阅只能推一条，因此**只在天气有明显变化或有特殊事项时才消耗这次机会**，不要每天推。

### 9.4 其他

| 能力 | 用途 |
|---|---|
| `wx.setStorageSync` | 缓存上一次同场景清单，支撑冷启动秒开 |
| `wx.vibrateShort` | 勾选时轻微震动反馈，提升「打卡感」 |
| 分享到聊天 | 出差/带娃清单分享给家人 |
| 「添加到我的小程序」引导 | 出门场景必须一步可达，首次使用后引导添加 |

---

## 10. 隐私与合规

### 10.1 数据最小化

| 数据 | 是否收集 | 说明 |
|---|---|---|
| openid | ✅ | 用户唯一标识，必需 |
| 昵称/头像 | ❌ | 不需要 |
| 精确定位 | ❌ | 只保留城市名 |
| 物品名称 | ✅ | 用户自填，是核心数据 |
| 出门时间/星期 | ✅ | 规律学习必需 |
| 通讯录/相册/日历 | ❌ | 完全不申请 |

### 10.2 必做项

1. **小程序隐私协议**：在微信后台配置《用户隐私保护指引》，声明收集 openid、位置（城市级）、用户自填内容。
2. **`wx.requirePrivacyAuthorize`**：基础库 2.32.3+ 起，调用位置等接口前必须完成隐私授权弹窗。
3. **数据出境说明**：如果 Claude API 走境外调用，需在隐私政策中明确告知「用户填写的物品与场景信息会用于生成清单，可能经由境外 AI 服务处理」，并提供关闭 AI 功能的开关（关闭后退化为纯规则引擎）。**这一条必须在上线前与法务确认。**
4. **脱敏**：发给 LLM 的 payload 中不包含 openid、不包含任何联系方式；物品名如包含疑似敏感词（药品名、病历），提供「本地处理」开关。
5. **数据删除**：「我的 → 设置 → 删除我的全部数据」，物理删除并在 7 天内清理备份。

---

## 11. 技术选型与工程结构

### 11.1 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 小程序 | 原生小程序 + TypeScript + Skyline | 首屏性能最优；本项目组件量少，不需要跨端框架 |
| 状态管理 | 轻量自研 store（约 100 行） | 页面少，引入 MobX 不划算 |
| 后端 | Python 3.12 + FastAPI | 与 AI SDK 生态一致，异步友好 |
| 数据库 | PostgreSQL 16 | JSONB 存权重/条件很合适；物化视图做画像 |
| 缓存 | Redis | 天气缓存、清单缓存、限流 |
| AI | Anthropic Python SDK (`anthropic`) | 官方 SDK，支持结构化输出与 prompt 缓存 |
| 部署 | 微信云托管 或 自有 K8s | 云托管省去备案与域名配置，MVP 阶段更快 |
| 监控 | Prometheus + Grafana | 重点看 P95 延迟与缓存命中率 |

### 11.2 目录结构

```
bieluole/
├── miniapp/                        # 小程序
│   ├── app.ts
│   ├── app.json
│   ├── pages/
│   │   ├── index/                  # 首页
│   │   ├── checklist/              # 清单页
│   │   ├── items/                  # 物品库
│   │   ├── routine/                # 规律
│   │   ├── history/                # 历史
│   │   └── profile/                # 我的
│   ├── components/
│   │   ├── go-out-button/          # 主按钮（含按压动效）
│   │   ├── checklist-group/        # 分级清单组
│   │   ├── item-row/               # 单项（勾选/长按菜单）
│   │   ├── scene-picker/
│   │   └── weather-bar/
│   ├── utils/
│   │   ├── request.ts              # 请求封装 + token 刷新 + 重试
│   │   ├── auth.ts
│   │   ├── local-rules.ts          # 客户端规则引擎（秒开骨架清单）
│   │   └── cache.ts
│   └── typings/
│
└── server/                         # 后端
    ├── app/
    │   ├── main.py
    │   ├── api/
    │   │   ├── auth.py
    │   │   ├── checklists.py
    │   │   ├── items.py
    │   │   ├── routines.py
    │   │   └── context.py
    │   ├── ai/
    │   │   ├── checklist.py        # Claude 调用（见 7.4）
    │   │   ├── prompts.py
    │   │   └── schema.py
    │   ├── rules/
    │   │   └── engine.py           # L1 规则引擎
    │   ├── learning/
    │   │   ├── stats.py            # 权重更新
    │   │   └── discover.py         # 周期规律挖掘
    │   ├── services/
    │   │   ├── weather.py
    │   │   └── wechat.py
    │   ├── models/                 # SQLAlchemy
    │   └── core/                   # 配置、日志、鉴权
    ├── migrations/                 # Alembic
    ├── tests/
    └── pyproject.toml
```

---

## 12. 性能、降级与容错

### 12.1 延迟预算

| 阶段 | 目标 | 手段 |
|---|---|---|
| 小程序冷启动 → 首屏可见 | ≤ 800ms | 分包、Skyline、首屏不请求网络 |
| 点击按钮 → 骨架清单可见 | ≤ 200ms | 本地规则引擎 + `wx.setStorageSync` 缓存 |
| 点击按钮 → AI 清单到达 | P95 ≤ 2.5s | `effort: low`、prompt 缓存、并行取天气 |
| 提交反馈 | 异步，不阻塞 UI | 乐观更新 + 失败重试队列 |

### 12.2 降级链路

```
AI 调用超时(2.5s) / 报错 / 限流
        │
        ├─→ 命中 Redis 中同用户同场景 24h 内的清单？ → 返回，标记 generated_by="cache"
        │
        ├─→ 否 → 返回 L1 规则引擎结果，标记 generated_by="rule"
        │
        └─→ 规则引擎也无结果（极端新用户）→ 返回身份标签对应的预置模板
```

**任何情况下都不返回错误页。** 出门场景下的空白页 = 用户流失。

### 12.3 错误处理要点（后端）

Anthropic SDK 的异常要分类处理，不要一把 `except Exception` 兜住：

```python
import anthropic

try:
    result = generate_checklist(profile, ctx, draft)
except anthropic.RateLimitError:
    metrics.incr("ai.rate_limited")
    return fallback_from_cache_or_rules(...)
except anthropic.APITimeoutError:
    metrics.incr("ai.timeout")
    return fallback_from_cache_or_rules(...)
except anthropic.APIStatusError as e:
    metrics.incr("ai.api_error", tags={"status": e.status_code})
    logger.error("claude api error", extra={"request_id": e.response.headers.get("request-id")})
    return fallback_from_cache_or_rules(...)
except anthropic.APIConnectionError:
    metrics.incr("ai.conn_error")
    return fallback_from_cache_or_rules(...)
```

另外注意：Claude Opus 5 的安全分类器在极少数情况下会返回 `stop_reason: "refusal"`（HTTP 200，`content` 为空或不完整）。读取 `content` 前先判断：

```python
if response.stop_reason == "refusal":
    metrics.incr("ai.refusal")
    return fallback_from_cache_or_rules(...)
```

对本项目而言这几乎不会触发（出门清单不涉及敏感领域），但代码必须能安全通过，否则 `content[0]` 会直接抛 IndexError。

### 12.4 关键监控指标

| 指标 | 告警阈值 |
|---|---|
| `checklist.generate.p95_latency` | > 3s |
| `ai.cache_read_ratio`（缓存读 token / 总输入 token） | < 0.5（说明前缀被污染了） |
| `ai.fallback_rate` | > 5% |
| `checklist.take_rate`（清单项被勾选比例） | < 0.4（说明推荐质量差） |
| `retention.d7` | — |

其中 `ai.cache_read_ratio` 是最容易悄悄劣化的指标：任何人往 system prompt 里加了一个时间戳，成本会直接翻数倍而功能完全正常，只能靠监控发现。

---

## 13. 成本估算

按每次生成的 token 用量估算（Claude Opus 5：输入 $5/MTok，输出 $25/MTok；缓存读 ≈ 输入价的 0.1×，缓存写 ≈ 1.25×）：

| 项 | token | 单价 | 成本 |
|---|---|---|---|
| 缓存命中的前缀（system + 画像） | 2000（缓存读） | $0.5/MTok | $0.0010 |
| 本次上下文（时间/天气/草稿） | 300（全价） | $5/MTok | $0.0015 |
| 输出（清单 JSON） | 450 | $25/MTok | $0.0113 |
| **合计** | | | **≈ $0.0138 / 次 ≈ ¥0.10** |

| 规模 | 日请求量（假设人均 1.5 次/天） | 日成本 | 月成本 |
|---|---|---|---|
| 1,000 DAU | 1,500 | ¥150 | ¥4,500 |
| 10,000 DAU | 15,000 | ¥1,500 | ¥45,000 |

**降本手段（按优先级）：**
1. 同用户同场景同天的重复请求走 Redis 缓存（预计可省 30–40%）。
2. 缩短输出：理由文案限 14 字、清单项限 15 项以内——输出 token 是成本大头。
3. 模式固定的日常通勤走纯规则引擎，只有天气突变 / 场景变化 / 新场景才调 AI（预计可省 50%+）。
4. 低价值场景切 `claude-haiku-4-5`。

第 3 条效果最好且不损失体验，建议 MVP 后立刻做。

---

## 14. 开发计划与里程碑

| 阶段 | 周期 | 交付内容 |
|---|---|---|
| **M0 原型** | 1 周 | 交互原型（首页 + 清单页）、Prompt 效果验证（拿 20 组真实场景手测 AI 输出质量） |
| **M1 MVP** | 3 周 | F-01 ~ F-08 全部完成，可提审上线。含：规则引擎、Claude 接入、物品库、天气、反馈闭环 |
| **M2 学习闭环** | 2 周 | F-09 ~ F-13：规律管理、习惯挖掘、历史记录、落东西复盘 |
| **M3 提醒与打磨** | 2 周 | 订阅消息、性能优化（P95 < 2.5s）、降级链路完善、埋点与监控看板 |
| **M4 增长** | 持续 | 分享海报、模板市场、多人场景 |

### M1 详细拆解（3 周）

| 周 | 前端 | 后端 |
|---|---|---|
| W1 | 首页 + 主按钮 + 天气条；请求封装；登录 | 建库建表；登录接口；天气服务 + 缓存 |
| W2 | 清单页 + 分级卡片 + 勾选交互；本地规则引擎 | L1 规则引擎；`checklists:generate`；Claude 接入 + 结构化输出 |
| W3 | 物品库 CRUD；冷启动引导流程；联调 | 反馈接口；统计更新；降级链路；压测 |

---

## 15. 测试与验收标准

### 15.1 功能验收

| 用例 | 预期 |
|---|---|
| 新用户完成引导后首次点击「我要出门」 | ≤ 2.5s 出清单，must 项 ≤ 4 项，无明显无关物品 |
| 下雨天（降水概率 > 40%）点击 | 伞类物品出现在 must 或 suggest，理由包含具体概率数字 |
| 周三 19:00 点击（已配置游泳规律） | 场景自动预选为「游泳」，泳具在 must |
| 断网点击 | 展示上次同场景缓存清单，顶部提示「离线清单」 |
| AI 服务不可用 | 返回规则引擎结果，用户无感知（不出现报错页） |
| 连续两次同场景请求 | 第二次 `cache_read_input_tokens > 0`（缓存生效） |
| 标记某物品「永久移除」 | 后续同场景不再出现 |
| 标记「我落了充电宝」 | 下次同场景充电宝在 must，理由含「上次落下过」 |

### 15.2 AI 质量评估

建立 30 组固定评测集（覆盖：工作日通勤 ×5、雨天 ×4、极端温度 ×4、健身 ×3、出差 ×4、带娃 ×4、看病 ×2、临时场景 ×4），每次改 Prompt 或换模型/effort 档位后回归：

| 指标 | 定义 | 目标 |
|---|---|---|
| 必带准确率 | must 项中人工判定「确实必须」的比例 | ≥ 90% |
| 漏报率 | 人工清单中有但 AI 未给出的关键物品占比 | ≤ 10% |
| 噪音率 | 清单中人工判定「完全不需要」的占比 | ≤ 15% |
| 理由有效性 | 理由包含具体依据（数值/历史/活动）的比例 | ≥ 80% |
| 结构合法率 | 输出符合 JSON Schema 的比例 | 100%（结构化输出保证） |

### 15.3 性能验收

- 首页冷启动 → 可交互：≤ 800ms（微信开发者工具性能面板，中端安卓机）
- `checklists:generate` P95：≤ 2.5s；P99：≤ 4s
- 后端单实例 QPS：≥ 50（规则路径）、≥ 10（AI 路径）

---

## 16. 未来迭代方向

1. **回家提醒**：反向场景——离开办公室/健身房时提醒「别落下水杯、雨伞、充电器」。这可能比出门场景更高频，因为在外面落东西才是真正的痛点。
2. **物品位置记忆**：「钥匙通常在玄关柜」，配合清单一起提示，减少「知道要带但找不到」的问题。
3. **家庭共享清单**：一个账号下多成员，出门时按谁出门生成不同清单。
4. **旅行打包模式**：从「出门 5 分钟」扩展到「出差前一晚打包」，清单更长、支持分类折叠和打包进度。
5. **主动感知**：结合微信运动步数或用户主动打卡，识别「已出门」状态，用于验证清单是否被真的执行。

---

## 附录 A：预置物品库（冷启动用）

| 分类 | 物品 |
|---|---|
| 随身必备 | 钥匙、手机、钱包、身份证、交通卡 |
| 工作 | 工牌、笔电、充电器、笔记本、名片 |
| 电子 | 充电宝、耳机、数据线、手表 |
| 天气 | 折叠伞、雨衣、墨镜、防晒霜、帽子、手套、围巾 |
| 健康 | 口罩、纸巾、湿巾、常用药、医保卡、水杯 |
| 运动 | 运动鞋、运动服、泳镜、泳帽、护具、健身卡 |
| 育儿 | 纸尿裤、奶瓶、湿巾、备用衣物、玩具、儿童水杯 |
| 出行 | 身份证、机票/车票、洗漱包、换洗衣物、拖鞋、充电器 |

## 附录 B：预置场景与默认参数

| code | 名称 | 默认时长 | 典型时段 | 关联分类 |
|---|---|---|---|---|
| `work` | 上班 | full_day | 工作日 7:00–9:30 | 随身、工作、电子 |
| `gym` | 健身/运动 | short | 6:00–8:00 / 18:00–21:00 | 随身、运动 |
| `errand` | 买菜/办事 | short | 全天 | 随身 |
| `kids` | 带娃 | half_day | 周末 | 随身、育儿、天气 |
| `travel` | 出差/旅行 | overnight | 全天 | 出行、电子、随身 |
| `hospital` | 看病 | half_day | 工作日上午 | 随身、健康 |
| `social` | 聚会/约会 | half_day | 傍晚 | 随身、电子 |

## 附录 C：环境变量

```bash
# 微信
WECHAT_APPID=
WECHAT_SECRET=

# Claude
ANTHROPIC_API_KEY=            # 或使用 `ant auth login` 配置的 profile
CLAUDE_MODEL=claude-opus-5
CLAUDE_EFFORT=low             # low | medium | high
CLAUDE_TIMEOUT_SECONDS=2.5

# 天气服务
WEATHER_API_KEY=
WEATHER_PROVIDER=qweather

# 基础设施
DATABASE_URL=postgresql://...
REDIS_URL=redis://...
JWT_SECRET=
AI_ENABLED=true               # 关闭后全量走规则引擎（用于降级与合规开关）
```
